请基于项目根目录的 `DESIGN.md` 与 `.cursor/rules/project-context.mdc`，为这个 AI 自由职业接单者个人网站执行正式构建。

目标不是做一个“好看但空”的模板，而是做一个能获取客户、展示能力、建立长期个人品牌的中文个人网站。

执行要求：

1. 先阅读并严格遵循以下文件，再开始写代码：
   - `DESIGN.md`
   - `.cursor/rules/project-context.mdc`

2. 如果当前项目没有既有技术栈，优先使用最简单、最容易维护的实现方式：
   - `index.html`
   - `styles.css`
   - `script.js`
   - 必要时增加 `assets/` 目录
   - 不要默认引入 React、Next.js、Tailwind、GSAP 等额外框架

3. 网站定位：
   - 站点类型：AI 自由职业者个人网站
   - 目标：获取客户、展示服务能力、打造长期个人品牌
   - 语言：中文
   - 受众：需要 AI 文案、Prompt 工程、自动化解决方案的客户

4. 首屏必须使用以下文案：
   - 主标题：`用AI帮你提升效率、降低成本、创造增长`
   - 副标题：`提供AI文案、Prompt工程、自动化解决方案`

5. 页面至少包含以下 section：
   - 导航栏
   - Hero 首屏
   - 服务能力
   - AI 技能方向
   - AI 作品 / 案例展示
   - 关于我 / 背景
   - 联系我
   - Footer

6. 内容策略：
   - 如果用户真实资料尚未提供，允许使用明确的中文占位内容
   - 不要伪造客户 logo、合作品牌、成交数据、案例结果
   - 作品区优先展示“问题 -> 方案 -> 结果”
   - 所有可替换的个人信息集中在一个容易修改的位置，例如顶部常量对象或独立数据文件

7. 视觉与交互要求：
   - 严格遵循 `DESIGN.md` 中的色板、字体、组件、动效、响应式规则
   - 仅实现 L1 精致静态动效
   - 保持深色专业感，不要做成赛博朋克或娱乐化视觉
   - 保证移动端无横向溢出，触摸目标不小于 `44px`
   - 所有交互元素必须有 hover 与 focus-visible 状态

8. 可访问性与语义化：
   - 使用语义化 HTML 结构
   - 图片必须有合理的 `alt`
   - CTA、导航、表单字段要有明确文案
   - 对比度满足深色背景下的可读性

9. 输出要求：
   - 直接创建可运行的页面文件
   - 页面加载后可直接预览
   - 不要先输出大段解释再动手
   - 完成后给出简短交付说明

10. 完成后额外输出一段简短影响分析，格式固定为：
    - 修改了什么 -> 影响了哪些模块 -> 需要同步更新哪些文档
